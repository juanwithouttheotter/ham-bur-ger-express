<h1>Ham Bur Ger Express</h1>

<h2> Burgers Ready to Devour! </h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers you have Devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>


<h2>Order a new burger </h2>
<form class="create-form">
  <div class="form-group">
    <label for="burger">Your new burger:</label>
    <input type="text" id="burger" name="name">
  </div>


</form>